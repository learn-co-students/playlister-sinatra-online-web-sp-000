<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1><%= @artist.name %></h1>

    <% @artist.songs.each do |song| %>
        <p><%= song.name %></p>
        <a href="/songs/<%= song.slug %>">view</a>
    <% end %>
    <% @artist.genres.each do |genre| %>
        <p><%= genre.name %></p>
        <a href="/genres/<%= genre.slug %>">view</a>
    <% end %>
</body>
</html>