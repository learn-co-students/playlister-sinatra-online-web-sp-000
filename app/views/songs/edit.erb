<!DOCTYPE html>
<html>
<head>
	<title>Edit Your Songs</title>
</head>
<body>
	<h1> Edit Songs </h1>
	
	<form action="/songs/<%= @song.slug %>" method="post">
    <input id="hidden" type="hidden" name="_method" value="patch">
  	
  	<label> Name:</label>
  	  <input type="text" id="Name" name="song[name]" value="<%=@song.name%>">
  	  
  	<label> Artist:</label>
  	  <input type="text" id="Artist Name" name="song[artist]" value="<%= @song.artist.name %>">
  	  
  	<label> Genres:</label>
  	 <% Genre.all.each do |genre| %>
      <input type="checkbox" id="<%= genre.name %>" name="song[genres][]" value="<%= genre.id %>" <%='checked' if @song.genres.include?(genre) %>> <!-- end of input --> 
      <%= genre.name %>
      </input>
  	<% end %> 
  	<br> 
  	<input type ="submit" value ="Save"> 
	</form> 
	
</body>
</html>