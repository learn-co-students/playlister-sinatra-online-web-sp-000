<!DOCTYPE html>
<html>
    <head>
        <title>SONG EDIT</title>
    </head>
    <body>
        <form action="/songs/:slug" method="post">
        <input id="hidden" type="hidden" name="_method" value="patch" >
        <label>Song Name</label>
        <input type="text" name="Name" value ="<%=@song.name%>"></input>
        <label>Artist Name</label>
        <input type="text" name="Artist Name" value="<%=@song.artist.name%>"></input>
        
        <%Genre.all.each do |genre|%>
        <input type="checkbox" name="genres[]" id="<%=genre.name%>" value="<%=genre.id%>" <%="checked" if @song.genres.include?(genre)%>><%=genre.name%></input>
        <%end%>
        <input type="submit" value="Save" id="Save">
    </body>
</html>