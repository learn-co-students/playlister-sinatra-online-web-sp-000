<h1>Edit Song</h1>
 
<form action="/songs/<%= @song.slug %>" method="POST">
  <input id="hidden" type="hidden" name="_method" value="patch">
  <label>Song Name: </label>
  <input type="text" name="song[name]" id="song_name" value="<%=@song.name%>"><br>
  
  <label>Artist: </label>
  <input type="text" name="artist[name]" id="artist_name" value="<%=@song.artist.name%>"></input><br>
  <br><br>
 
  <br>
  <p>Select an existing genre or create a new one below.</p>
 
 
  <h3>Existing Genres</h3>
  <% Genre.all.each do |genre| %>
    <input type="checkbox" name="genres[]" id="<%= genre.id %>" value="<%= genre.id %>" <%='checked' if @song.genres.include?(genre) %>><%= genre.name %></input><br>
  <% end %>
  <br>
 
 
  <input type="submit" value="Edit Song">
</form>