Edit <%=@song.name%>


<form method="POST" action="/songs/<%=@song.slug%>">
  <input id="hidden" type="hidden" value="patch" name="_method">
  
  <label for="name">Name</label> 
  <input type='text' name='song[name]' id='name' value="<%= @song.name %>">
  
  
  <label for="artist_name">Artist Name</label> 
  <input type='text' name='artist[name]' id='artist_name' value="<%= @song.artist.name %>">
  
  
  Edit Genre: 
  <% Genre.all.each do |genre| %>
    <label><%=genre.name%></label>
  <input type="checkbox" id="<%=genre.name%>" <%= "checked" if @song.genres.include?(genre)%> value="<%=genre.id%>" name="genres[]">
  <% end  %>
  
  <input type="submit" value="Save">
  
</form>