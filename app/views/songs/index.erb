<!DOCTYPE html>
<html>
  <head>
    <title>All the songs!</title>
  </head>

  <body>
    <% @songs.each do |song| %>
      <h2><%= song.name %></h2>
      <a href="/songs/<%= song.slug %>"><%= song.name %></a>
    <% end %>
  </body>
</html>
