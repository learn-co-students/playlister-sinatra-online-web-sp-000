<!DOCTYPE html>
<html>
  <head>
    <title>Playlister</title>
  </head>

  <body>
    <% @songs.each do |song| %>
      <a href="/songs/<%= song.slug %>"><%= song.name %></a><br>
    <% end %>
  </body>
</html>