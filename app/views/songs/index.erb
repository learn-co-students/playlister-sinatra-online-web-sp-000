<!DOCTYPE html>
<html>
  <head>
    <title>songs</title>
  </head>

  <body>
    <h1>Available Songs</h1>
        <ul>
            <% @songs.each do |song| %>
                <li><h4><a href="/songs/<%= song.slug %>"><%= song.name %></h4></li></a>
            <% end %>
        </ul>    
  </body>
</html>