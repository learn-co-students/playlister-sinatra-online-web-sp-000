<% if flash.has?(:message) %>
  <%= flash[:message] %>
<% end %>

<h1>Song: <%= @song.name %></h1>
<h3>by <a href="/artists/<%= @artist.slug %>"><%= @artist.name %></a></h3>
<h3>Genre(s): </h3>
  <ul>
    <% @song.genres.each do |genre| %>
    <li><a href="/genres/<%= genre.slug %>"><%= genre.name %></a></li>
    <% end %>
  </ul>
