<!DOCTYPE html>
<html>
<head>
  <title><%= @song.name %></title>
</head>
<body>
  <% if @new_song_message %>
    <h1>Successfully created song.</h1>
    <br />
  <% end %>
  <% if @changed_song_message %>
    <h1>Successfully updated song.</h1>
    <br />
  <% end %>
  <h1><%= @song.name %>, by <a href="/artists/<%=@song.artist.slug%>">
    <%= @song.artist.name %></a></h1>
  <h2>Genres</h2>
  <ul>
    <% @song.genres.each do |genre| %>
    <li><a href="/genres/<%=genre.slug%>"><%= genre.name %></a></li>
    <% end %>
  </ul>
</body>
</html>
