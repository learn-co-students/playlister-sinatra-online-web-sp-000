<!DOCTYPE html>
<html>
  <head>
    <title>Sinatra Playlister Lab - Songs</title>
    <style>
    body {background-color: black;}
    h1 {color: silver; font-family: verdana;}
    p {color: silver; font-family: verdana;}
    a {text-decoration: none; color: powderblue;}
    </style>

  </head>

  <body>
  <% if flash.has?(:message) %>
  <%= flash[:message] %>
  <% end %>
 
  <h1><%= @song.name %> by <a href="/artists/<%= @song.artist.slug %>"><%= @song.artist.name %></a></h1>
  <% @song.genres.each do |genre| %>
    <p><a href="/genres/<%= genre.slug %>"><%= genre.name %></a></p>
  <% end %>
  </body>
</html>