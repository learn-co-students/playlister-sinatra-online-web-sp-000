song show page

<% if flash.has?(:message) %>
  <%= flash[:message] %>
<% end %>

<%artist_slug = @song.artist.slug%>

<%= @song.name%>
<%= @song.artist.name %>

<a href="/artists/<%=artist_slug%>"><%=@song.artist.name%></a>

<%@song.genres.each do |genre|%>
  <%genre_slug = genre.slug%>
  <%=genre.name%>
  <a href="/genres/<%=genre_slug%>"><%= genre.name%></a>
<%end%>
