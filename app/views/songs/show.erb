<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Song Page</title>
        <style>
            .card {
                background-color: #ccc;
                text-align: center;
                width: 70%;
                margin: 7% auto;
                padding: 10px;
                border: 1px solid #555;
                box-shadow: -1px 1px #444, -2px 2px #333, -3px 3px 5px #222;
                border-radius: 5px;
            }
        </style>
    </head>
    <body>
        <div class="card">
            <%if session[:message] != ""%>
            <h3><%=session[:message]%></h3>
            <%end%>
            <h1>Song Name: <%=@song.name%></h1>
            <div class="artist_column">
                <h2>Artist:</h2>
                <h4><%=@song.artist.name%></h4>
                <a href="/artists/<%=@song.artist.slug%>">Artist Page</a>
            </div>
            <div class="genre_column">
                <h2>Genre:</h2>
                <%@genres.each do |genre_object|%>
                    <h4><%=genre_object.name%></h4>
                    <a href="/genres/<%=genre_object.slug%>">Genre Page</a>
                <%end%>
            </div>
        </div>
        
    </body>
</html>