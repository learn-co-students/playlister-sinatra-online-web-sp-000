<h1>Create a New Song</h1>

<form action='/songs' method='POST'>
    Song Name: <input type="text" name='song[name]' id="Name"><br><br>
    Artist Name: <input type="text" name='song[artist]' id="Artist Name"><br><br>
    Genres:<br>
    <% @genres.each do |genre| %>
        <input type="checkbox" name="song[genres][]" id="<%=genre.name%>" value="<%=genre.id%>" ><%=genre.name%><br>
     <% end %>
    <br><input type="submit" value="Create">
</form>