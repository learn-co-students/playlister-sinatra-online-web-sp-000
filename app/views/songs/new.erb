<h1>Create a New Song</h1>
 
<form action="/songs" method="POST">
  <label>Song Name: </label>
  <input type="text" name="song[name]" id="song_name"><br>
  
  <label>Artist: </label>
  <input type="text" name="artist[name]" id="artist_name"></input><br>
  <br><br>
 
  <br>
  <p>Select an existing genre or create a new one below.</p>
 
 
  <h3>Existing Genres</h3>
  <% @genres.each do |genre| %>
    <input type="checkbox" name="genres[]" id="<%= genre.id %>" value="<%= genre.id %>"><%= genre.name %></input><br>
  <% end %>
  <br>
 
 
  <input type="submit" value="Create Song">
</form>