<!DOCTYPE html>
<html>
<head>
  <title>New Song</title>
</head>
<body>
  <form method="POST" action="/songs">
    <p>
    <label for="song[name]">Song Name:</label>
      <input type="text" name="song[name]" id="song[name]">
    </p>
    <label for="genre">Genre:</label>
    <% @genres.each do |genre| %>
      <input type="checkbox" name="song[genre_ids][]"
      id="<%= genre.id %>" value="<%= genre.id %>">
        <%= genre.name %></input>
    <% end %>
    <p>
      <label for="artist">Artist Name:</label>
      <input type="text" name="song[artist][name]" id="song[artist][name]">
    </p>
    <input type="submit" id="submit" value="Create Song">
  </form>
</body>
</html>
