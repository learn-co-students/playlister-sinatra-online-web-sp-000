<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1><%= @genre.name %></h1>

    <% @genre.songs.each do |song| %>
        <p><%= song.name %></p>
        <a href="/songs/<%= song.slug %>">view</a>
    <% end %>
    <% @genre.artists.each do |artist| %>
        <p><%= artist.name %></p>
        <a href="/artists/<%= artist.slug %>">view</a>
    <% end %>
</body>
</html>