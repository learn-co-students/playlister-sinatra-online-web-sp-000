<% if flash.has?(:message) %>
  <%= flash[:message] %>
<% end %>

<h1>Info for <%= @genres.name %> </h1>

<h3> Artists </h3>
  <p>
      <% @genres.artists.each do |artist| %>
    <li><a href="/artists/<%=artist.slug%>"><%=artist.name%></a></li>
  </p>
    <% end %>

  <h3> Songs </h3>
    <p>
      <% @genres.songs.each do |song| %>
      <li><a href="/songs/<%=song.slug%>"><%=song.name%></a></li>
      <% end %>
    </p>
