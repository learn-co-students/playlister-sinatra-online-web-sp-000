<!DOCTYPE html> 
<html>
<head>
    <title>Genre Info Page</title>
</head>

<body>
    <h1><%= @genre.name %></h1>

    <h3>Songs:</h3>
    <ul id="song_name"><% @genre.songs.each do |song| %>
        <li><a href="/songs/<%= song.slug %>"><%= song.name %></li>
    <% end %></ul>

    <h3>Artists:</h3>
    <ul id="artist_name"><% @genre.artists.each do |artist| %>
        <li><a href="/artists/<%= artist.slug %>"><%= song.name %></li>
    <% end %></ul> 

</body> 

</html>