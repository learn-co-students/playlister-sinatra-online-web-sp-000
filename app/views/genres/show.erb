<!DOCTYPE html>
<html>
  <head>
    <title> <%=@genre.name%> </title>
  </head>

  <body>
    <h1> <%=@genre.name%> </h1>
   
    <h2>Songs:</h2>
    <ul>
        <% @genre.songs.each do |song| %>
        <li><a href="/songs/<%=song.slug%>"><%=song.name%><a></li>
        <% end %>
    </ul>
    <br>
    <br>

    <h2>Artists:</h2>
    <ul>
        <% @genre.artists.each do |artist| %>
        <li><a href="/artists/<%=artist.slug%>"><%=artist.name%><a></li>
        <% end %>
    </ul>
      
   
    <br>
    <br>
    ToDo: Edit Genre Button Eventually
  </body>
</html>