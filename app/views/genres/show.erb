<h1>Genre Name: <%= @genre.name %> </h1>
<h2>Genre Songs: <% @genre.songs.each do |song| %> 
<br><a href="/songs/<%= song.slug%>"> <%= song.name %></a>
<% end %></h2>
<h2>Genre Artists: <% @genre.artists.each do |artist| %> 
<br><a href="/artists/<%= artist.slug%>"> <%= artist.name %></a>
<% end %>
</h2>

