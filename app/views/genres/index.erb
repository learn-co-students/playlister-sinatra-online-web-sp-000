<!DOCTYPE html>
<html>
  <head>
    <title>Playlister</title>
  </head>

  <body>
    <% @genres.each do |genre| %>
      <a href="/genres/<%= genre.slug %>"><%= genre.name %></a><br>
    <% end %>
  </body>
</html>