<!DOCTYPE html>
    <html>
        <head>
            <title>Genre Library</title>
        </head>
        <body>
            <% @genres.each do |genre| %>
                <a href="/genres/<%= genre.slug %>"> <%= genre.name %></a> =%>
            <% end %>
        </body>
    </html>
            