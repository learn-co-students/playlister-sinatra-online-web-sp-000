<!DOCTYPE html>
<html>
  <head>
    <title>All the songs!</title>
  </head>

  <body>
    <% @genres.each do |genre| %>
      <h2><%= genre.name %></h2>
      <a href="/genres/<%= genre.slug %>"><%= genre.name %></a>
    <% end %>
  </body>
</html>
