<% if !@genres.blank? %>
<h1>Genres</h1>
<ol>
  <% @genres.each do |genre|  %>
  <li><a href="/genres/<%= genre.id%>"><%= genre.name %></a></li>
  <% end %>
</ol>
<% else %>
<h2>You have no genres yet.</h2>
<% end %>
