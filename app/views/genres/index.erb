<!DOCTYPE html>
<html>
<head>
  <title>Genres</title>
</head>
<body>
  <h1>Genres:</h1>
  <% @genres.each do |genre| %>
    <p><a href="/genres/<%= genre.slug %>"><%= genre.name %></a></p>
  <% end %>
</body>
</html>
