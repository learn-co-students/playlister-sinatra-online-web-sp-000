<h1>Artist Name: <%= @artist.name %> </h1>
<h2>Artist Songs: <% @artist.songs.each do |song| %> 
<br><a href="/songs/<%= song.slug%>"> <%= song.name %></a>
<% end %>
<h2>Artist Genre(s): <% @artist.genres.each do |genre| %> 
<br><a href="/genres/<%= genre.slug%>"> <%= genre.name.titleize %></a>
<% end %>
</h2>

