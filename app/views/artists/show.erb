<!DOCTYPE html> 
<html>
<head>
    <title>Artist Info Page</title>
</head>

<body>
    <h1>Artist:</h1>

    <p><%= @artist.name %></p>

    <h3>List of songs:</h3>
    <ul id="song_name"><% @artist.songs.each do |song| %> 
        <li><a href="/songs/<%= song.slug %>"><%= song.name %></a></li> 
    <% end %></ul>

    <h3>List of genres:</h3>
    <ul id="genre_name"><% @artist.genres.each do |genre| %>
        <li><a href="/genres/<%= genre.slug %>"><%= genre.name %></a></li>
    <% end %></ul> 
</body> 

</html>