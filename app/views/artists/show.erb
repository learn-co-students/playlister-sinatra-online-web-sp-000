<h1><%= @artist.name %></h1>
<% @artist.songs.each do |song| %>
  <label><a href="/songs/<%= song.slug %>"><%= song.name %>: </a></label>
  <% song.genres.each do |genre| %>
    <label><a href="/genres/<%= genre.slug %>"><%= genre.name %></a></label>
  <% end %>
<% end %>