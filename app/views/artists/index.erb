<!DOCTYPE html>
<html>
  <head>
    <title>Playlister</title>
  </head>

  <body>
    <% @artists.each do |artist| %>
      <a href="/artists/<%= artist.slug %>"><%= artist.name %></a><br>
    <% end %>
  </body>
</html>