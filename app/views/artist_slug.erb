<section>
  <ul>
    <% @slug_artist.songs.each do |s| %>
      <li><a href="/songs/<%= s.slug %>"><%= s.name %> </a></li>
    <% end %>
  </ul>
</section>

<section>
  <ul>
    <% @slug_artist.genres.each do |g| %>
      <li><a href="/genres/<%= g.slug %>"><%= g.name %> </a></li>
    <% end %>
  </ul>
</section>
